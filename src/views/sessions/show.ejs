<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title><%= session.sportId.name %> Session - Sports Scheduler</title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
	<link rel="stylesheet" href="/css/sessions.css">
</head>
<body>
	<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
		<div class="container">
			<a class="navbar-brand" href="/">Sports Scheduler</a>
			<div class="navbar-nav ms-auto">
				<a class="nav-link" href="/sessions">Sessions</a>
			</div>
		</div>
	</nav>

	<div class="container mt-4">
		<div class="card p-4">
			<h2 class="mb-3"><%= session.sportId.name %> session</h2>
			<p>
				<strong>When:</strong> <%= new Date(session.startsAt).toLocaleString() %><br>
				<strong>Where:</strong> <%= session.venue %><br>
				<strong>Status:</strong> <span class="badge bg-secondary"><%= session.status %></span>
			</p>

			<% if (session.status === 'cancelled' && session.cancelReason) { %>
				<p class="text-danger"><strong>Cancelled:</strong> <%= session.cancelReason %></p>
			<% } %>

			<h3 class="mt-4">Participants</h3>
			<ul class="list-group mb-3">
				<% participants.forEach(p => { %>
					<li class="list-group-item"><%= p.userId.name %> <%= p.teamSlot ? '(' + p.teamSlot + ')' : '' %></li>
				<% }) %>
			</ul>

			<% if (currentUser) { %>
				<% if (currentUser.role === 'player') { %>
					<% 
						let userRequest = null;
						if (participants) {
							userRequest = participants.find(p => p.userId._id.toString() === currentUser._id.toString());
						}
					%>

					<% if (!userRequest) { %>
						<form method="post" action="/sessions/<%= session._id %>/join">
							<button type="submit" class="btn btn-primary">
								<i class="fas fa-handshake me-2"></i>Request to Join
							</button>
						</form>
					<% } else { %>
						<% if (userRequest.status === 'pending') { %>
							<div class="alert alert-warning">Your request to join this session is pending admin approval.</div>
						<% } else if (userRequest.status === 'approved') { %>
							<div class="alert alert-success">You are approved to join this session!</div>
						<% } else if (userRequest.status === 'rejected') { %>
							<div class="alert alert-danger">Your request to join this session was rejected.</div>
						<% } %>
					<% } %>

				<% } else if (currentUser.role === 'admin') { %>
					<% if (session.status !== 'cancelled') { %>
						<div class="alert alert-info">Admin View: You can manage this session and approve/reject join requests.</div>
						<form method="post" action="/sessions/<%= session._id %>/cancel" onsubmit="return confirm('Are you sure you want to cancel this session?')">
							<div class="mb-3">
								<label for="reason" class="form-label">Cancellation Reason</label>
								<input type="text" class="form-control" id="reason" name="reason" placeholder="Enter reason for cancellation" required>
							</div>
							<button type="submit" class="btn btn-danger">Cancel Session</button>
						</form>
					<% } else { %>
						<div class="alert alert-danger">This session has been cancelled.</div>
					<% } %>
				<% } %>
			<% } else { %>
				<div class="alert alert-info">Please <a href="/auth/user-login">login</a> to request joining this session.</div>
			<% } %>

			<div class="mt-3">
				<a href="/sessions" class="btn btn-secondary">Back</a>
			</div>
		</div>
	</div>

	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>




