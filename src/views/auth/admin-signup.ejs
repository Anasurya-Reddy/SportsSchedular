<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Signup - Sports Scheduler</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .signup-container {
            min-height: 100vh;
            background: linear-gradient(135deg, #dc3545 0%, #6f42c1 100%);
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .signup-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            padding: 2rem;
            width: 100%;
            max-width: 400px;
        }
        .btn-danger {
            background: linear-gradient(135deg, #dc3545 0%, #6f42c1 100%);
            border: none;
        }
        .btn-danger:hover {
            background: linear-gradient(135deg, #c82333 0%, #5a2d91 100%);
        }
    </style>
</head>
<body>
    <div class="signup-container">
        <div class="signup-card">
            <div class="text-center mb-4">
                <h2 class="fw-bold">Admin Signup</h2>
                <p class="text-muted">Create admin account to manage sports and sessions</p>
            </div>

            <% if (flash.error && flash.error.length > 0) { %>
                <div class="alert alert-danger" role="alert">
                    <%= flash.error[0] %>
                </div>
            <% } %>

            <form method="POST" action="/auth/admin-signup">
                <div class="mb-3">
                    <label for="name" class="form-label">Full Name</label>
                    <input type="text" class="form-control" id="name" name="name" required>
                </div>
                <div class="mb-3">
                    <label for="email" class="form-label">Email</label>
                    <input type="email" class="form-control" id="email" name="email" required>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" class="form-control" id="password" name="password" required>
                </div>
                <div class="mb-3">
                    <label for="adminCode" class="form-label">Admin Code</label>
                    <input type="text" class="form-control" id="adminCode" name="adminCode" placeholder="Enter admin code" required>
                    <small class="form-text text-muted">Contact system administrator for admin code</small>
                </div>
                <button type="submit" class="btn btn-danger w-100 mb-3">Sign up as Admin</button>
            </form>

            <div class="text-center">
                <p class="mb-2">Already have an account? <a href="/auth/admin-login">Login as Admin</a></p>
                <p class="mb-0">Are you a regular user? <a href="/auth/user-login">User Login</a></p>
            </div>
        </div>
    </div>
</body>
</html>
